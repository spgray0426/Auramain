// Copyright Druid Mechanics

/**
 * EnemyInterface.cpp
 *
 * 적 인터페이스 클래스 구현 파일
 *
 * 적 캐릭터 전용 기능을 정의하는 인터페이스입니다.
 * 적 AI의 행동 및 전투 로직에 필요한 함수들을 선언합니다.
 *
 * 주요 기능 (헤더에 선언):
 * - SetCombatTarget(): 전투 대상 설정 (AI가 공격할 목표)
 * - GetCombatTarget(): 현재 전투 대상 반환
 *
 * 적 캐릭터(AuraEnemy)가 이 인터페이스를 구현합니다.
 */

#include "Interaction/EnemyInterface.h"

// 순수 가상 함수가 아닌 IEnemyInterface 함수들의 기본 구현을 여기에 추가

