// Copyright Druid Mechanics

/**
 * CombatInterface.cpp
 *
 * 전투 인터페이스 클래스 구현 파일
 *
 * 전투 관련 기능을 정의하는 인터페이스입니다.
 * 캐릭터 클래스들이 이 인터페이스를 구현하여 공통된 전투 기능을 제공합니다.
 *
 * 주요 기능 (헤더에 선언):
 * - GetPlayerLevel(): 플레이어/적 레벨 반환
 * - GetCombatSocketLocation(): 무기 소켓 위치 반환 (투사체 발사 위치)
 * - GetHitReactMontage(): 피격 반응 몽타주 반환
 * - IsDead(): 사망 여부 확인
 * - GetAvatar(): 액터 반환
 * - GetOnASCRegisteredDelegate(): ASC 등록 델리게이트 반환
 * - GetOnDeathDelegate(): 사망 델리게이트 반환
 *
 * 순수 가상 함수가 아닌 함수들의 기본 구현을 여기에 추가할 수 있습니다.
 */

#include "Interaction/CombatInterface.h"

// 순수 가상 함수가 아닌 ICombatInterface 함수들의 기본 구현을 여기에 추가

